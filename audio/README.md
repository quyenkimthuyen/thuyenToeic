Put optional local word audio files here, for example `abandon.mp3`, then set `"audio_us": "audio/abandon.mp3"` in a topic JSON file under `data/`.

You can also use a public internet audio URL directly, for example `"audio_us": "https://example.com/audio/abandon.mp3"`.
